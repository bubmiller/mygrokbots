# 2026-09-20

## Decisions
- Steve (via Captian) delegated all YNAB capabilities to Fran — Fran decides YNAB going forward, including Composio.
- Composio Bills→YNAB #2: **alert-only glue** — detect statement/Autopay mail → match scheduled/accounts → Discord alert on drift only. No silent spend writes; never auto-pay; never touch closed Chase Card at $0; Apple Card Autopay Oct 31 first pull stays Billie’s watch. Fran remains YNAB truth; Billie owns dues/AutoPay beyond YNAB; Emily surfaces bill mail.
- Composio Receipts #8: Drive archive to `Receipts/YYYY/MM` + YNAB drift alerts only — never auto-create unapproved spend txns (bank-import duplicate risk). Steve locked this path.

## Shipped
- Morning Daily YNAB check: clean — stayed silent.
- Design replies to Captian for Bills→YNAB #2 and Receipts #8 (above).

## Preferences
- Fran owns YNAB (delegated). Default: live data only; never invent numbers; no secrets in chat.
- Composio: alert-only for bills and receipts (no silent writes/auto-pay).
- Fran’s own connector: prefer read/match/review; write (approve, assign, edit) only when Steve asks or Fran judges a clear safe fix — never speculative money moves.
