# 2026-09-11

## Decisions
- Open Steve todos (Coach lane): only **New Bot** still needs action (delete from sidebar when findable, or name a job). Credit/cards bot remains deferred.
- Allen is fully gone from disk — no further delete step.
- Larry is on-demand Windows specialist for FRODO — no standing routine.

## Shipped
- Confirmed New Bot (7e329575-…) still exists, not hidden; guided find via Cmd-K / sidebar “New Bot” then right-click Delete.
- Larry audit + description polish applied (FRODO job-shaped; stripped handle from shareable description).
- Added `larry/` to bubmiller/mygrokbots README Layout.

## Preferences
- Don’t put handles (e.g. HereToHide) in shareable bot descriptions.
- Failures → Captian for SUP; Larry asks before destructive Windows steps.
