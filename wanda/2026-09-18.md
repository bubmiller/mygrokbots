# 2026-09-18

## Decisions
- Left live post 1952 (OpenAI Valuation IPO / pre-filing compute) as-is — no rewrite draft, no live edit.
- Steve asked Wanda to improve the AI Bot **generation prompt** using 1952 as the failure case (not to rewrite that post).
- Steve approved publishing two rewrite drafts by swapping onto live posts (same URLs), then trashing drafts.

## Shipped
- Swapped rewrite drafts onto live posts (slugs unchanged; author steve; drafts trashed):
  - Live 1937 — AI data centers / environmental polls (from draft 1938)
  - Live 1934 — Saudi East-West pipeline → retail gas/diesel angle (from draft 1940; distinct from earlier systems/failover piece 1865)
- Evaluated newest post 1952 for Captian: ~472 words, AI Bot byline, no citations, keyword-stuffed lead/title → verdict **needs rewrite** (Steve then chose leave as-is + fix prompt).
- Delivered generation prompt **v2** (hardened gates). Copy saved on box at `/workspace/wanda-rewrites/generation-prompt-v2.md`.

## Standing preferences
- Generation prompt v2 for themillerscloud.com: Steve Miller voice (Mason OH ops); **800–1,200 words**; **2–4 real clickable citations**; no invented facts/URLs/stats/valuations; human Title Case (no keyword sausage in title or lead); **author always `steve` never AI Bot**; `featured_image_alt` when schema allows; clean HTML (no H1 in body); minified JSON `{title,content,category,author,featured_image_alt}`.
- Draft-first for content swaps; publish/overwrite live only after Steve’s explicit OK.
- Site hard rule unchanged: no publish/delete/plugin installs without Steve’s OK; theme changes OK; failures → Captian; never open SUP or pay.
